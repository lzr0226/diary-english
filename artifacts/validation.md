# 验证结果

验证日期：2026-09-11。环境：Windows、Node.js 24.19.0、Next.js 15.5.25、React 19.3.0。

- ESLint：通过，0 errors / 0 warnings。命令：`node node_modules/eslint/bin/eslint.js .`。
- TypeScript：`tsc --noEmit` 通过；production build 中的类型检查同样通过。
- 业务测试：6/6 通过。覆盖保存、采用历史、生词去重与语境、复习间隔、账号隔离、存储损坏和失败保护。
- Production build：通过。App 首次加载 JS 约 145 kB；真实 AI API 默认关闭。
- Production server：`next start --hostname 127.0.0.1 --port 3000` 启动成功。
- 浏览器：Playwright + 本机 Microsoft Edge，独立上下文，未使用用户正常浏览器数据。
- 完整流程：登录 → 新建 → 自动保存 → 生成结果未改变英文 → 确认采用 → 生词数保持不变 → 确认添加后入库 → 复习写日志 → 助手对话 → 刷新保留数据，通过。
- 恢复场景：模拟 AI 故障保留原文、设置关闭故障开关、原文更改后旧 AI 结果按钮禁用、退出确认可取消、退出重登仍保留词汇，通过。
- 响应式：320 / 390 / 1280px 三种宽度，对日记、生词、助手、我的、新建页面共 15 组横向溢出检查，通过。
- 浏览器运行时异常：0。
- 已检查本文件夹中的登录、列表、详情、新建、生词本、复习、助手、个人中心及桌面截图。

截图为隔离自动化账号的状态，其中包含“浏览器闭环测试”等测试日记；正常浏览器首次打开仍是 4 篇种子日记、12 个生词。

未接入真实 Supabase/DeepSeek，因此不宣称已验证真实认证、RLS 线上行为、模型效果或跨设备同步。没有部署公开网址。
