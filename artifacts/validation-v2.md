# 2026-09-12 优化验证

- ESLint：通过，0 errors / 0 warnings。
- 业务测试：13/13 通过，包含原 6 项及新增润色、词形解析、文档候选提取、照片关联、反馈编码、LAN UUID 回归。
- Production build：通过，TypeScript 检查通过，首屏 JS 约 155 kB，PDF/Word 解析库按需加载。
- 原闭环浏览器测试：通过；15 组响应式检查、AI 采用/词汇确认、复习、助手、数据刷新保持正常。
- 新增浏览器测试：通过；实际读取 PDF 和 DOCX 内容并查询中文；词义弹窗无须填写输入；采用前英文不变，采用后发生改写；IndexedDB 图片在保存/刷新后可读，个人相册可见；反馈草稿刷新保留；新增页面 9 组响应式检查通过。
- 新增测试在生产服务器的 WLAN 地址 `http://10.7.68.176:3000` 通过；浏览器运行时错误 0。
- 独立 LAN 测试确认 `isSecureContext=false`、`crypto.randomUUID` 和 `crypto.subtle` 均不可用时，仍可注册新账号、创建和自动保存日记。
- 监听验证：`0.0.0.0:3000`。当前 WLAN IP：`10.7.68.176`，以太网 IP：`192.168.166.208`。
- 本机对 WLAN 地址的访问已验证；没有直接操作用户手机，不能证明跨设备网络、防火墙或 AP 隔离情况。
- 创建持久入站规则的请求被自动审批拒绝，未修改防火墙。规则拟限定 Node 可执行文件、TCP 3000、本地子网；当前网络为 Public，需要用户明确授权后才能继续该设置。
- SMTP 未配置，直接发信未做真实联调，也未发送测试邮件。当前反馈入口使用邮件客户端发送，服务端 SMTP 通道及环境配置已备好。
- Supabase 和 DeepSeek 仍未启用。英文改写是有限本地规则，非通用 AI；词义来自本地 ECDICT，非实时上下文消歧。

截图：`word-meaning-v2.png`、`import-pdf-v2.png`、`import-docx-v2.png`、`album-v2.png`、`feedback-v2.png`。测试使用独立浏览器上下文，不修改用户正常浏览器里的日记。
