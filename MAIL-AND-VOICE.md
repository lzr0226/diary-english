当前网站使用腾讯云 Web 部署，完整配置及验收见 [TENCENT-WEB.md](TENCENT-WEB.md)。以下发信与语音服务说明继续适用；小程序部署已暂停。

# 网页直接发信与语音输入

## 直接发送反馈

“发送反馈”现在调用服务端接口，不自动跳到邮件应用。只有 SMTP 服务器接受邮件才显示成功；没有配置或失败时返回明确错误并保留草稿。收件人固定为 `1363578991@qq.com`，只发送用户填写的表单，不附带日记、账号令牌或生词。

在 Vercel **Production** 环境变量中配置以下项，然后重新部署。小程序需要在 `diaryApi` 云函数中独立配置同样的项目：

```dotenv
SMTP_HOST=smtp.qq.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=实际发信邮箱@qq.com
SMTP_PASSWORD=邮箱SMTP授权码
SMTP_FROM=实际发信邮箱@qq.com
```

发信邮箱不一定要与收件邮箱相同；`SMTP_FROM` 应与已认证邮箱相符。QQ 邮箱需要在邮箱设置开启 SMTP 并取得**授权码**，不要填写 QQ 登录密码。其他邮箱按服务商提供的 SMTP 主机、端口与 TLS 配置填写。这不是 Supabase Auth 的注册邮件配置，两者互不替代。

本地检查可将变量存入 `.env.local`（不要提交），运行：

```bash
pnpm check:services
```

此命令只检查变量、SMTP 连接和认证，不发送邮件、不输出密钥。真实送达需用登录账号从反馈页提交一条明确标注“发信测试”的消息并检查收件箱/垃圾箱。Web 端不允许未登录的公网邮件转发，反馈额度为每用户每天 5 次。

## 腾讯云语音输入

Web 和小程序使用同一个腾讯云 ASR 服务适配层。需要在腾讯云开通“一句话识别”，准备仅有 `asr:SentenceRecognition` 权限的凭据，并设置：

```dotenv
TENCENT_SECRET_ID=
TENCENT_SECRET_KEY=
TENCENT_REGION=ap-guangzhou
```

以上变量只放到 Vercel 服务端和/或腾讯云函数环境变量，**不要添加 NEXT_PUBLIC 前缀**。开通服务、可用额度与 IAM 权限都需要在真实环境验证。

Web：使用 HTTPS 或 localhost，并登录云端账号；浏览器需支持麦克风和 Web Audio。最长 45 秒录音，浏览器转换为 16kHz 单声道 WAV，停止后交由服务端识别。支持中文和英文选择，识别结果可修改并确认追加，不自动覆盖正文。页面离开会停止麦克风。Web 语音请求与 AI 共用原有每天 30 次额度，无需为它另跑 SQL。未配置服务、本地 demo、无权限或不支持录音时显示原因，不伪造识别结果；手机输入法语音仍可使用。

小程序：微信录音管理器录制 MP3，停止后通过云函数发送腾讯 ASR。需要小程序后台的隐私保护指引、`requirePrivacyAuthorize` 与录音权限。ASR 每用户每天 30 次，和小程序 AI 额度独立。

音频不保存到应用数据库或云存储，仅本次传输和识别使用。网络失败仍保留原日记文字；识别结果确认前不会加入正文。

## 本次交付与上线区分

源码、适配层和本地 SMTP 模拟服务器测试不能替代真实邮箱授权码、ASR 服务权限或小程序 AppID。若环境变量尚未提供，网站仍不能真正发信或识别语音。修改本地文件后还需更新 Vercel 部署才能在线上生效；小程序按 `tencent/README.md` 单独部署和审核。

